# tests/ — pytest suite

pytest suite mirroring the packages. Run with `python -m pytest`; coverage HTML → `_coverage_html/`.

## Contents

- `test_honeybee_revive/`, `test_ladybug_revive/`, and per-package folders mirroring the source layout.

## Notes
- Serialization round-trip and the pandas-based `output/` reporting are both under test — see `../context/CODING_STANDARDS.md`.
